fastapi
uvicorn
pydantic
google-genai
chromadb
sentence-transformers
pypdf
reportlab
python-docx
pandas
# Version validée avec pandas 1.5.3, numba 0.58 et ydata-profiling. Ne pas
# laisser pip remonter en numpy 2.x : les binaires pandas/numba compilés contre
# numpy 1.x lèvent alors « numpy.dtype size changed » et tout le backend casse.
numpy==1.26.4
numba<0.59.0
setuptools<70
seaborn
# Version alignée sur celle du sandbox (backend/sandbox/requirements.txt).
# Le tournoi supervisé sérialise son pipeline DANS le sandbox et le backend le
# recharge pour la simulation : un écart de version casse le dépicklage
# (« Can't get attribute '_RemainderColsList' »). Les deux fichiers doivent
# donc être modifiés ensemble.
scikit-learn==1.5.2
statsmodels
openpyxl
python-dotenv
python-multipart
pdfplumber
rouge-score
bert-score
httpx
ydata-profiling==4.15.1
docker
pandasai
# ATTENTION : `timecopilot` ne doit PAS figurer ici. Ses contraintes sont
# incompatibles avec deux dépendances de ce venv, sans version commune possible :
#   - timecopilot exige transformers<5, colpali-engine exige transformers>=5.3
#   - timecopilot exige openai>=1.99.7, pandasai 0.4.0 exige openai<0.28
# L'installer ici rétrograde transformers et casse l'indexation visuelle
# (ColSmolVLM). Il vit dans un venv dédié : voir requirements-timecopilot.txt.
tf-keras
openai-whisper
soundfile
torch<2.12.0
certifi
colpali-engine
pymupdf
# OCR local des PDF scannés (moteur ONNX, aucune dépendance système).
# onnxruntime et opencv sont épinglés : leurs versions récentes exigent
# numpy 2.x, incompatible avec le reste du stack.
rapidocr-onnxruntime
onnxruntime==1.19.2
opencv-python==4.10.0.84